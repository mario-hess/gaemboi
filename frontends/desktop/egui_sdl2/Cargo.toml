[package]
name = "egui_sdl2"
version = "0.1.0"
edition = "2024"

[dependencies]
egui_sdl2_gl = "0.31.0"
sdl2 = { version = "0.37.0", features = ["bundled", "static-link"] }
gaemboi = { path = "../../../lib" }
ringbuf = "0.4.7"

[profile.release]
lto = true
codegen-units = 1
